import { FuenteVeinteDirective } from './fuente-veinte.directive';

describe('FuenteVeinteDirective', () => {
  it('should create an instance', () => {
    const directive = new FuenteVeinteDirective();
    expect(directive).toBeTruthy();
  });
});
